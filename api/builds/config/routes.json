{
  "routes": [
    {
      "method": "GET",
      "path": "/builds",
      "handler": "builds.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/builds/count",
      "handler": "builds.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/builds/:id",
      "handler": "builds.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/builds",
      "handler": "builds.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/builds/:id",
      "handler": "builds.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/builds/:id",
      "handler": "builds.delete",
      "config": {
        "policies": []
      }
    }
  ]
}

{
  "routes": [
    {
      "method": "GET",
      "path": "/weapon-types",
      "handler": "weapon-types.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/weapon-types/count",
      "handler": "weapon-types.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/weapon-types/:id",
      "handler": "weapon-types.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/weapon-types",
      "handler": "weapon-types.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/weapon-types/:id",
      "handler": "weapon-types.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/weapon-types/:id",
      "handler": "weapon-types.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
